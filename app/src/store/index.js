import Vue from 'vue'
import Vuex from 'vuex'

// Install Vuex module into Vue
Vue.use(Vuex)

export default new Vuex.Store({
    modules: {
        // Modules go here from ./modules/someModule.js
    }
})